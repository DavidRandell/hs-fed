# HubSpot Submission - David Randell

## Instructions

Clone this repo and run `npm install`.

Run `gulp build` to build the `build` directory

Run `gulp start` the server will serve the site on `localhost:3000`.